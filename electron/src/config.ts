// Update this URL after deploying to Railway (or any host).
// You can also override it at build time via the LIVESTUDIO_URL environment variable.
export const SERVER_URL =
  process.env.LIVESTUDIO_URL ?? 'https://your-app.railway.app';
